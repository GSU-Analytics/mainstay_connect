# remote_install.yaml

# To install this environment, use the following command:
# conda env create -f remote_install.yaml
# After creating the environment, activate it with:
# conda activate mainstay_connect

name: mainstay_connect
channels:
  - defaults
dependencies:
  - python>=3.10
  - pip
  - pip:
    - git+https://github.com/GSU-Analytics/mainstay_connect.git
